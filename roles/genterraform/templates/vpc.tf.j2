{% for bus_unit in business_units %}
resource "aws_vpc" "{{ bus_unit.vpc.name }}" {
 cidr_block = "{{ bus_unit.vpc.cidr }}"
 
 tags = {
   generated: "VPC Terraform resource {{ bus_unit.vpc.name }} generated by Ansible"
   Name = "{{ bus_unit.vpc.name }}"
 }
}
{% endfor %}
